apiVersion: v1
kind: Service
metadata:
  name: fitness-service
spec:
  type: NodePort
  selector:
    app: fitness
    version: blue   # 👈 initially pointing to BLUE
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007