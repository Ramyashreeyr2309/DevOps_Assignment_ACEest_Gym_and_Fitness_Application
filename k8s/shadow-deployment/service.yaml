# k8s/shadow/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: fitness-service
spec:
  type: NodePort
  selector:
    app: fitness
    version: stable   # 👈 ONLY stable serves users
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007