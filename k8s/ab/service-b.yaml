# k8s/ab/service-b.yaml
apiVersion: v1
kind: Service
metadata:
  name: fitness-service-b
spec:
  type: NodePort
  selector:
    app: fitness
    version: v2
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30008