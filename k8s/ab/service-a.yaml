# k8s/ab/service-a.yaml
apiVersion: v1
kind: Service
metadata:
  name: fitness-service-a
spec:
  type: NodePort
  selector:
    app: fitness
    version: v1
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007